<Window x:Class="berles2.DeviceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:berles2"
        mc:Ignorable="d"
        Title="Eszköz adatok" Height="650" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Címsor -->
            <RowDefinition Height="*"/>
            <!-- Főtartalom -->
            <RowDefinition Height="Auto"/>
            <!-- Gombok -->
        </Grid.RowDefinitions>

        <!-- CÍMSOR -->
        <TextBlock Grid.Row="0" x:Name="TitleTextBlock" Text="Új eszköz hozzáadása" 
                   FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" 
                   Margin="0,0,0,20" Foreground="#2E7D32"/>

        <!-- ADATOK -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Eszköz neve -->
                <Label Content="Eszköz neve *:" FontWeight="Bold"/>
                <TextBox x:Name="DeviceNameTextBox" Height="30" Margin="0,0,0,10"/>

                <!-- Eszköz típusa -->
                <Label Content="Eszköz típusa *:" FontWeight="Bold"/>
                <ComboBox x:Name="DeviceTypeComboBox" Height="30" Margin="0,0,0,10" 
                         DisplayMemberPath="TypeName" SelectedValuePath="Id"/>

                <!-- Sorozatszám -->
                <Label Content="Sorozatszám:" FontWeight="Bold"/>
                <TextBox x:Name="SerialTextBox" Height="30" Margin="0,0,0,10"/>

                <!-- Árak -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <Label Content="Vételár (Ft):" FontWeight="Bold"/>
                        <TextBox x:Name="PriceTextBox" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <Label Content="Bérlési ár (Ft/nap) *:" FontWeight="Bold"/>
                        <TextBox x:Name="RentPriceTextBox" Height="30"/>
                    </StackPanel>
                </Grid>

                <!-- Elérhető checkbox -->
                <CheckBox x:Name="AvailableCheckBox" Content="Elérhető bérlésre" 
                         IsChecked="True" FontWeight="Bold" Margin="0,0,0,15"/>

                <!-- Kép -->
                <Label Content="Eszköz képe:" FontWeight="Bold"/>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="PicturePathTextBox" Grid.Column="0" Height="30" 
                            IsReadOnly="True" Background="#F0F0F0"/>

                    <Button x:Name="BrowsePictureButton" Grid.Column="2" Content="Tallózás..." 
                           Width="80" Height="30" Click="BrowsePictureButton_Click"/>
                </Grid>

                <!-- Kép előnézet -->
                <Border x:Name="ImagePreviewBorder" Height="150" Margin="0,0,0,10"
                       BorderBrush="Gray" BorderThickness="1" Background="#F9F9F9">
                    <Image x:Name="ImagePreview" Stretch="Uniform" Margin="5"/>
                </Border>

                <!-- Megjegyzések -->
                <Label Content="Megjegyzések:" FontWeight="Bold"/>
                <TextBox x:Name="NotesTextBox" Height="80" 
                        TextWrapping="Wrap" AcceptsReturn="True" 
                        VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>

                <TextBlock Text="* Kötelező mezők" FontSize="12" 
                          Foreground="Red" FontStyle="Italic" Margin="0,10,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <!-- GOMBOK -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                   HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="SaveButton" Content="Mentés" 
                    Width="80" Height="35" Margin="0,0,10,0"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"
                    Click="SaveButton_Click"/>

            <Button x:Name="CancelButton" Content="Mégsem" 
                    Width="80" Height="35"
                    Background="#757575" Foreground="White" FontWeight="Bold"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>